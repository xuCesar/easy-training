# Issue 30 实施计划

1. 创建并完成“教学仓储兼容与并发保障”子任务。
   - 定位 `normalizeRoom` 与现有冲突检测；补 Unicode 规范化及集成测试。
   - 以教学集成测试证明补课、教室写入和排课在并发下仍遵守容量、启停与冲突规则。
   - 提交 1：仅包含仓储与测试改动。
2. 创建并完成“教务工作区渐进拆分”子任务。
   - 将工作区容器保留为 tab、组织上下文和查询编排；将课程/教师/班级/课次面板及编辑器按现有 feature 目录拆出。
   - 不改 URL、ORPC 路径、查询 key 语义、tab 门控或分页方式。
   - 提交 2：仅包含无行为变化的结构重组及必要类型检查修复。
3. 创建并完成“教务交互可靠性与无障碍”子任务。
   - 在拆分组件上补查询错误状态/重试、危险对话框 pending 守卫、编辑 `requestId` 会话管理及 a11y 名称。
   - 补现有测试框架支持范围内的回归测试。
   - 提交 3：仅包含可见交互与可访问性修复。
4. 总体验收。
   - 运行相关类型检查、Biome 检查、前端构建与教学集成测试。
   - 在允许 `tsx` IPC 的环境运行 `pnpm test:integration`；若当前受限环境仍阻断，记录该限制并保留已运行的静态验证。
   - 更新 #30：按每个提交记录完成项，全部完成后关闭 Issue。

## 风险检查点

- `packages/db/src/repositories/*`：不得改变锁顺序、事务边界或历史文本写入。
- `apps/web/src/features/training/academic-workspace.tsx`：拆分期间不得重新引入不按 tab 门控的查询或移除 cursor 分页。
- 对话框关闭行为：必须同时覆盖按钮、Escape、点击遮罩与提交失败后的恢复状态。
