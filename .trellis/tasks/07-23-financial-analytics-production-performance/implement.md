# 实施计划

## 当前进度（2026-07-23）

- 已创建隔离的本地 `easy-training-mock`，应用全部迁移并生成 20 万账单、20 万归属事实、139,911 笔收款、6,938 笔冲正、4,187 笔退款和 1,939 条调整。
- 已固化可重复 seed 与 explain 脚本。两年范围 mock 验证显示，资金下钻从约 62ms 降至约 1.8ms，并确认正式 `payment_org_received_id_invoice_idx` 被使用。
- 账龄下钻约 110ms，仍有结算聚合临时磁盘 I/O；实验账单索引没有被查询计划采用，因此未加入仓库。
- 已通过 `pnpm test:integration`（79/79）、`pnpm check-types`、`pnpm check`、`pnpm build` 和 `git diff --check`。
- 尚需生产等量只读副本执行同一 explain，作为最终上线结论；mock 结果不能替代该步骤。

1. 创建 `easy-training-mock`，应用全部已提交 migration，并确认开发数据库未受影响。
2. 在 `packages/db` 增加可配置且幂等的合成财务数据生成器；默认档位覆盖多个机构、校区、两年账单和完整资金/调整事实。
3. 增加独立性能执行器：为全机构/单校区、summary/两类下钻、最长两年范围记录 JSON explain 摘要。
4. 运行 mock 数据生成与性能执行器，记录数据规模、执行计划、耗时、共享缓冲区和初始结论。
5. 仅在证据表明有慢路径时提出索引改动；否则记录“维持现有索引”。
6. 运行相关类型检查、lint、测试与构建；mock 结果不替代生产只读副本的最终门禁。

## 回滚

- 代码改动可正常回退；mock 数据库独立存在，可在明确确认后删除。
- 不修改默认开发数据库或生产配置。
