# 教学仓储兼容与并发保障实施计划

1. 读取现有 lesson、classroom 与 makeup 集成测试 fixture 和 helper，复用既有创建路径。
2. 添加全角/半角历史教室冲突回归测试，明确断言持久化原文不变。
3. 添加最小的教室/补课/排课竞争测试；只有在测试暴露真实违反现有契约时才修改仓储。
4. 运行 DB 测试 TypeScript 检查和目标集成测试；再运行适用的 workspace 静态检查。
5. 单独提交该子任务，并在 #30 留下提交关联。
